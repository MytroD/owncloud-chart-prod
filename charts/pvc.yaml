apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-private-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: your-storage-class-name  # Specify your storage class
  resources:
    requests:
      storage: 10Gi
